import * as express from "express";
import HeroModel from "../models/hero";
import { HeroViewModel } from "../viewModels/hero";

export const create = (
  request: express.Request,
  response: express.Response,
  next,
) => {};

export const edit = (
  request: express.Request,
  response: express.Response,
  next,
) => {};

export const deleteOne = (
  request: express.Request,
  response: express.Response,
  next,
) => {};

export const getAll = (
  request: express.Request,
  response: express.Response,
  next,
) => {};

export const getOne = (
  rrequest: express.Request,
  response: express.Response,
  next,
) => {};
